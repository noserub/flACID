# Supabase SQL migrations

Run these files **in order** in the Supabase Dashboard → **SQL Editor** (they are not applied automatically from this repo).

All migration scripts live in **`supabase/migrations/`** at the project root:

- `001_initial_schema.sql` … `006_storage_admin_rls.sql`

See **`SUPABASE_SETUP.md`** in the repo root for the full checklist and admin setup.
