/**
 * Re-exports auth from context — one subscription app-wide.
 */

export { useAuth, AuthProvider } from '../contexts/AuthContext';
export type { AuthContextValue } from '../contexts/AuthContext';
