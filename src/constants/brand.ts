/**
 * flACID brand tokens — re-exported from colorValues (synced with globals.css).
 */
export { BRAND_COLORS, COLOR_VALUES } from '../lib/colorValues';

export const HERO_COPY = {
  ctaLabel: 'Listen',
  phonetic: "/flæs'id/",
} as const;
